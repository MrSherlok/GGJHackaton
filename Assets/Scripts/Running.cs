using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Running : MonoBehaviour {
    // Update is called once per frame
    void Update()
    {
        //if (transform.position.x == -7)
        //{
        //    transform.rotation = Quaternion.Slerp(transform.rotation, Quaternion.Euler(0, 0, 0), Time.deltaTime * 3f);
        //}
        //if (transform.position.x == 5)
        //{
        //    transform.rotation = Quaternion.Slerp(transform.rotation, Quaternion.Euler(0, 180, 0), Time.deltaTime * 3f);
        //}
        //if (transform.rotation.y == 0)
        //{
        //    transform.position = Vector3.Lerp(new Vector3(-7f, -3.85f, 0f), new Vector3(5f, -3.85f, 0f), Time.deltaTime*3f);
        //}
        //if (transform.rotation.y == 180)
        //{
        //    transform.position = Vector3.Lerp(new Vector3(5f, -3.85f, 0f), new Vector3(-7f, -3.85f, 0f), Time.deltaTime*3f);
        //}
    }
}
